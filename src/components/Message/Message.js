import React, { PropTypes } from 'react';

import './message.scss';

const Message = props => {
  return (
    <div className="message-container">
      I'm a message
    </div>
  );
}

Message.propTypes = {
  
};

export default Message;