import React, { PropTypes } from 'react';

import './chat-box.scss';

const ChatBox = props => {
  return (
    <div>
      I'm a ChatBox
    </div>
  );
}

ChatBox.propTypes = {
  
};

export default ChatBox;